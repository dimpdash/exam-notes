const noEvent = () => {/*Do nothing*/};

class Pointer {

    constructor(onMouseDown = noEvent, onMouseUp = noEvent, onMouseMove = noEvent,
        onTouchDown = noEvent, onTouchUp, on){
        this.onMouseDown = this.onMouseDown
    }
}